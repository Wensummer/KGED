"""
Thesis-oriented KG error detection pipeline.

This package reuses the existing internal/external KB assets under the repo,
but keeps the new template library and staged early-stop logic separate from
the original rag_kge experiments.
"""
